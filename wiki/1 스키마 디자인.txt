실제 구현은 sugang/home/models.py에서 확인 가능
과제 가이드라인과 동일
단 학생에서 password 제거 이는 보안상 바람직하지 않다.
student 를 읽을 때는 csv 라이브러리로 수동으로 읽어서 암호화된 계정 정보와 학생정보를 별도 테이블에 저장한다.
set_password(raw_password) 함수를 이용하면 원시 패스워드를 바탕으로 해싱된 패스워드를 생성해준다.
Django는 PBKDF2 알고리즘으로 SHA256 해시를 제공한다.

# 티어가 낮은 모델은 더 높은 티어의 모델에 의존적이다.
# 즉, DB 삽입 명령을 수행할 때 높은 티어부터 실행해야한다.
# Tier 1 Model: 전공, 건물, 과목
# Tier 2 Model: 교강사, 강의실
# Tier 3 Model: 학생, 수업
# Tier 4 Model: 성적, 수업시간, +auth_user

